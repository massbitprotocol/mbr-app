export default {
  setCurrentEra(state, currentEra) {
    state.currentEra = currentEra;
  },

  setChainToken(state, chainToken) {
    state.chainToken = chainToken;
  },

  setChainDecimal(state, chainDecimal) {
    state.chainDecimal = chainDecimal;
  },
};
